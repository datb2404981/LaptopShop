<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Account</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f5f6fa;
      min-height: 100vh;
      position: relative;
    }
    .bg-gradient-bottom {
      position: fixed;
      left: 0;
      bottom: 0;
      width: 100vw;
      height: 180px;
      background: #f5f6fa; /* đổi thành màu xám nhạt */
      z-index: 0;
    }
    .card-signup {
      border-radius: 20px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.1);
      padding: 32px 24px;
      background: #fff;
      z-index: 1;
      position: relative;
    }
    .signup-title {
      color: #218c5b; /* màu chữ tiêu đề giống navbar */
      font-weight: 700;
    }
    .btn-gradient {
      background: #218c5b; /* màu nút giống navbar */
      color: #fff;
      border: none;
    }
    .btn-gradient:hover {
      opacity: 0.9;
      color: #fff;
      background: #176943;
    }
    .btn-light {
      background: #f5f6fa !important; /* màu xám nhạt */
      color: #218c5b !important; /* chữ xanh lá */
      border: 1px solid #218c5b !important;
    }
    .btn-light:hover {
      background: #e2e6ea !important;
      color: #176943 !important;
      border: 1px solid #176943 !important;
    }
  </style>
</head>
<body>
  <div class="bg-gradient-bottom"></div>
  <div class="d-flex justify-content-center align-items-center" style="min-height: 80vh; position: relative; z-index: 1;">
    <div class="card-signup w-100" style="max-width: 400px;">
      <h2 class="text-center mb-4 signup-title">Create Account</h2>
      <form action="/createUser" method="post">
        <div class="mb-3 input-group">
          <span class="input-group-text bg-light"><i class="bi bi-person"></i></span>
          <input type="text" class="form-control" placeholder="Name" name="name" required>
        </div>
        <div class="mb-3 input-group">
          <span class="input-group-text bg-light"><i class="bi bi-envelope"></i></span>
          <input type="email" class="form-control" placeholder="E-mail" name="email" required>
        </div>
        <div class="mb-4 input-group">
          <span class="input-group-text bg-light"><i class="bi bi-geo-alt"></i></span>
          <input type="text" class="form-control" placeholder="Address" name="address" required>
        </div>
        <div class="d-flex gap-2">
          <button type="submit" class="btn btn-gradient flex-fill">SIGN UP</button>
          <button type="button" class="btn btn-light flex-fill">SIGN IN</button>
        </div>
      </form>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
</body>
</html>
